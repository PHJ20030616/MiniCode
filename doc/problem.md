- [ ] 现在执行上下文压缩的逻辑是：除了设置的需要保留的部分外（system prompt）其余部分是直接丢弃的
- [ ] 上下文压缩的时机是：每次调用LLM时

解决方案：（仿ClaudeCode）

- 清除上下文中Bash，Glob，Grep,File_read等命令读取的内容
- 后台开一个子Agent在不断进行总结，将总结的内容放在“备忘录”文件中
- 开一个子Agent，将需要压缩的上下文给子Agent，让其执行总结任务，最后使用总结的内容代替原来的上下文
- 触发时机：上下文窗口占用达到90%时/执行/compact
- [ ] 面向Git的安全改动流
- [x] 缺少调用子Agent的能力（但是目前还是实现的最简单的subagent，并且UI效果不太好）
- [x] 无法删除记忆
- [x] 无法像ClaudeCode一样先生成任务计划再按照计划执行---问题：UI不太好
- [x] 目前当模型调用工具时，是串行调用，串行执行的，优化为：按照原始工具调用顺序分块，分块原则是相邻的read命令可以分在同一块，写命令独占一块，当块内全部是读命令时--并发执行（并发上线为3），达到快速读取的目的，当块内是写命令时--串行执行，块间串行执行